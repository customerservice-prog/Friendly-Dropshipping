'use client';
import { FormEvent,useState } from 'react';
import { SiteHeader } from '@/components/site-header';
export default function Quote(){const [done,setDone]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/quotes',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),firstName:f.get('firstName'),lastName:f.get('lastName'),company:f.get('company'),phone:f.get('phone'),notes:`REQUESTED EQUIPMENT:\n${f.get('equipment')}\n\nDESTINATION / TIMING:\n${f.get('destination')}\n\nNOTES:\n${f.get('notes')}`,items:[]})});if(r.ok)setDone(true)}return <main><SiteHeader/><div className="container-shell py-12"><div className="mx-auto max-w-3xl"><div className="text-sm font-bold uppercase tracking-widest text-orange">Business sales</div><h1 className="mt-2 text-4xl font-black text-navy">Get a commercial equipment quote</h1><p className="mt-4 text-lg leading-8 text-slate-600">Tell us what you are trying to buy. We will verify the product, supplier availability, volume pricing and shipping instead of guessing.</p>{done?<div className="mt-8 rounded-2xl border bg-white p-10 text-center"><h2 className="text-2xl font-black text-navy">Quote request received</h2><p className="mt-3 text-slate-600">Your request is now in the quote system for review.</p></div>:<form onSubmit={submit} className="mt-8 grid gap-4 rounded-2xl border bg-white p-6 sm:grid-cols-2">{[['firstName','First name'],['lastName','Last name'],['email','Email'],['phone','Phone'],['company','Company']].map(([n,l])=><label key={n}><span className="text-xs font-black uppercase">{l}</span><input required={n==='email'} name={n} className="mt-2 w-full rounded-lg border px-4 py-3"/></label>)}<label className="sm:col-span-2"><span className="text-xs font-black uppercase">Equipment + quantities</span><textarea required name="equipment" placeholder="Example: 100 white resin chairs, 10 six-foot tables..." className="mt-2 min-h-28 w-full rounded-lg border px-4 py-3"/></label><label className="sm:col-span-2"><span className="text-xs font-black uppercase">Ship-to ZIP / needed-by date</span><input name="destination" className="mt-2 w-full rounded-lg border px-4 py-3"/></label><label className="sm:col-span-2"><span className="text-xs font-black uppercase">Anything else?</span><textarea name="notes" className="mt-2 min-h-24 w-full rounded-lg border px-4 py-3"/></label><button className="sm:col-span-2 rounded-lg bg-orange px-6 py-4 font-black text-white">Send quote request</button></form>}</div></div></main>}
