import { notFound } from "next/navigation";
import Link from "next/link";
import { Check, ChevronRight, Package, ShieldCheck, Truck } from "lucide-react";
import { SiteHeader } from "@/components/site-header";
import { AddToCart } from "@/components/add-to-cart";
import { money, products } from "@/lib/catalog";

export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <main><SiteHeader/><div className="container-shell py-8"><div className="mb-7 flex items-center gap-2 text-xs text-slate-500"><Link href="/">Home</Link><ChevronRight className="h-3 w-3"/><Link href={`/category/${p.category}`}>{p.category}</Link><ChevronRight className="h-3 w-3"/><span>{p.name}</span></div><div className="grid gap-10 lg:grid-cols-2"><div><div className="flex aspect-square items-center justify-center rounded-2xl bg-gradient-to-br from-slate-100 to-slate-200 text-center text-4xl font-black text-slate-400">{p.imageLabel}</div><div className="mt-3 rounded-lg border border-amber-200 bg-amber-50 p-3 text-xs font-semibold text-amber-900">Product photography is published only after supplier image rights and the exact SKU are verified.</div></div><div><div className="text-sm font-bold uppercase tracking-widest text-orange">Commercial equipment</div><h1 className="mt-3 text-4xl font-black leading-tight text-navy">{p.name}</h1><p className="mt-4 text-lg leading-8 text-slate-600">{p.summary}</p><div className="mt-6 text-sm font-bold text-amber-700">● {p.availability}</div><div className="mt-2 text-4xl font-black">{money(p.price)}</div><div className="text-sm text-slate-500">Displayed equipment price is provisional until the supplier SKU/cost is verified. Freight is confirmed before fulfillment.</div><AddToCart slug={p.slug} name={p.name} price={p.price}/><div className="mt-6 grid grid-cols-3 gap-3 text-center text-xs font-bold">{[[Truck,"Freight Review"],[Package,"Parts Support"],[ShieldCheck,"Verification Gate"]].map(([Icon,x]:any)=><div className="rounded-lg border p-3" key={x}><Icon className="mx-auto mb-2 h-5 w-5 text-orange"/>{x}</div>)}</div></div></div><div className="mt-14 grid gap-8 lg:grid-cols-2"><section><h2 className="text-2xl font-black text-navy">Proposed package</h2><div className="mt-4 divide-y rounded-xl border">{p.included.map(x=><div className="flex items-center gap-3 p-4" key={x}><Check className="h-5 w-5 text-emerald-600"/>{x}</div>)}</div><p className="mt-3 text-xs leading-5 text-slate-500">Exact included components are confirmed against the selected supplier listing before an order is approved.</p></section><section><h2 className="text-2xl font-black text-navy">Specifications</h2><div className="mt-4 overflow-hidden rounded-xl border">{Object.entries(p.specs).map(([k,v])=><div className="grid grid-cols-2 border-b last:border-0" key={k}><div className="bg-slate-50 p-4 text-sm font-bold">{k}</div><div className="p-4 text-sm">{v}</div></div>)}</div></section></div><section className="mt-14 rounded-2xl bg-navy p-8 text-white"><h2 className="text-2xl font-black">Buying for a rental company?</h2><p className="mt-3 max-w-2xl leading-7 text-slate-300">Get a business quote for multiple units, compatible accessories, replacement parts and freight.</p><Link href="/quote" className="mt-5 inline-block rounded-lg bg-white px-5 py-3 font-black text-navy">Request Commercial Quote</Link></section></div></main>}
