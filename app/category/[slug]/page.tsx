import { notFound } from "next/navigation";
import { SlidersHorizontal } from "lucide-react";
import { SiteHeader } from "@/components/site-header";
import { ProductCard } from "@/components/product-card";
import { categories, products } from "@/lib/catalog";

export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const category=categories.find(c=>c.slug===slug);if(!category)notFound();const items=products.filter(p=>p.category===category.slug);return <main><SiteHeader/><section className="relative min-h-[330px] overflow-hidden bg-navy"><img src={category.image} alt={`${category.name} commercial event equipment`} className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-0 bg-gradient-to-r from-[#071827]/95 via-[#071827]/70 to-transparent"/><div className="container-shell relative flex min-h-[330px] flex-col justify-center text-white"><div className="text-sm font-bold uppercase tracking-widest text-orange">Commercial equipment</div><h1 className="mt-2 text-5xl font-black">{category.name}</h1><p className="mt-3 max-w-2xl text-slate-200">{category.kicker}. Compare business-focused equipment, specifications, fulfillment details and compatible accessories.</p></div></section><div className="container-shell grid gap-8 py-10 lg:grid-cols-[240px_1fr]"><aside className="hidden lg:block"><div className="mb-5 flex items-center gap-2 font-black"><SlidersHorizontal className="h-5 w-5"/>Filters</div>{["Equipment type","Size / capacity","Commercial use","Availability","Shipping method","Price"].map(x=><div className="border-t py-4 text-sm font-bold" key={x}>{x}</div>)}</aside><section><div className="mb-6 flex items-center justify-between"><span className="text-sm text-slate-500">{items.length} catalog item{items.length===1?"":"s"}</span><button className="rounded-lg border px-4 py-2 text-sm font-bold lg:hidden">Filters</button><select className="hidden rounded-lg border px-4 py-2 text-sm md:block"><option>Recommended</option><option>Price: Low to High</option><option>Price: High to Low</option></select></div>{items.length?<div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{items.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="overflow-hidden rounded-2xl border bg-white"><img src={category.image} alt={`${category.name} equipment`} className="h-64 w-full object-cover"/><div className="p-8 text-center"><h2 className="text-xl font-black">More {category.name.toLowerCase()} are being added</h2><p className="mt-2 text-slate-500">This department stays visual even while additional verified supplier SKUs are prepared.</p></div></div>}</section></div></main>}
