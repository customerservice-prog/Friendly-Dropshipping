import { prisma } from '@/lib/prisma';
export const dynamic='force-dynamic';
export default async function Quotes(){const quotes=await prisma.quote.findMany({include:{customer:true,_count:{select:{items:true}}},orderBy:{createdAt:'desc'},take:100});return <main className="min-h-screen bg-slate-100"><div className="container-shell py-10"><div className="text-sm font-bold uppercase tracking-widest text-orange">Sales</div><h1 className="mt-2 text-3xl font-black text-navy">Quote & freight review queue</h1><div className="mt-8 space-y-4">{quotes.length?quotes.map(q=><article className="rounded-xl border bg-white p-5" key={q.id}><div className="flex flex-wrap items-center justify-between gap-3"><div><b className="text-navy">{q.number}</b><div className="text-sm text-slate-500">{q.customer.company||[q.customer.firstName,q.customer.lastName].filter(Boolean).join(' ')||q.customer.email} · {q.customer.email}</div></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-black">{q.status}</span></div><pre className="mt-4 whitespace-pre-wrap rounded-lg bg-slate-50 p-4 font-sans text-sm leading-6 text-slate-700">{q.notes}</pre></article>):<div className="rounded-xl border bg-white p-8 text-center text-slate-500">No quote requests yet.</div>}</div></div></main>}
