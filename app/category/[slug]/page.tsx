import { notFound } from "next/navigation";
import { SlidersHorizontal } from "lucide-react";
import { SiteHeader } from "@/components/site-header";
import { ProductCard } from "@/components/product-card";
import { categories, products } from "@/lib/catalog";

export default function CategoryPage({params}:{params:{slug:string}}){const category=categories.find(c=>c.slug===params.slug);if(!category)notFound();const items=products.filter(p=>p.category===category.slug);return <main><SiteHeader/><section className="bg-sand py-12"><div className="container-shell"><div className="text-sm font-bold uppercase tracking-widest text-orange">Commercial equipment</div><h1 className="mt-2 text-4xl font-black text-navy">{category.name}</h1><p className="mt-3 max-w-2xl text-slate-600">{category.kicker}. Compare business-focused equipment, specifications, fulfillment details and compatible accessories.</p></div></section><div className="container-shell grid gap-8 py-10 lg:grid-cols-[240px_1fr]"><aside className="hidden lg:block"><div className="mb-5 flex items-center gap-2 font-black"><SlidersHorizontal className="h-5 w-5"/>Filters</div>{["Equipment type","Size / capacity","Commercial use","Availability","Shipping method","Price"].map(x=><div className="border-t py-4 text-sm font-bold" key={x}>{x}<div className="mt-3 text-xs font-normal leading-6 text-slate-500">Filter values will be driven by verified structured product attributes.</div></div>)}</aside><section><div className="mb-6 flex items-center justify-between"><span className="text-sm text-slate-500">{items.length} catalog item{items.length===1?"":"s"}</span><button className="rounded-lg border px-4 py-2 text-sm font-bold lg:hidden">Filters</button><select className="hidden rounded-lg border px-4 py-2 text-sm md:block"><option>Recommended</option><option>Price: Low to High</option><option>Price: High to Low</option></select></div>{items.length?<div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{items.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="rounded-2xl border border-dashed p-12 text-center"><h2 className="text-xl font-black">Supplier catalog coming next</h2><p className="mt-2 text-slate-500">This category is ready for verified supplier products.</p></div>}</section></div></main>}
