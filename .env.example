# PostgreSQL connection
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/friendly_supply"

# Add payment, email and supplier credentials only when those integrations are enabled.
# Never commit real secrets to GitHub.
